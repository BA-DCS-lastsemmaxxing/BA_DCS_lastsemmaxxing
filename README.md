# BA_DCS_lastsemmaxxing

To run frontend:
    - Navigate to /frontend/my-app
    - Run "npm run dev"
    - Open localhost:3000 in browser

To run backend:
    - Navigate to /backend
    - Run "python app.py"
